# Normalise text files, but keep shell scripts LF-only so they stay
# executable on Linux/macOS after a checkout on Windows.
* text=auto

*.sh   text eol=lf
*.py   text eol=lf
*.xml  text eol=lf
*.md   text eol=lf
*.ps1  text eol=crlf

# Meshes are binary - never touch them.
*.stl  binary

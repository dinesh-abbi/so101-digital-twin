# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# MuJoCo
MUJOCO_LOG.TXT

# Machine-specific: each arm has its own calibration, never commit one
calibration/
*_follower.json

# Recorded data
datasets/
recordings/
*.mp4
debug_*.csv
trace.csv

# Machine-specific setup run logs
*_log.txt

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Conference-room capture sources (128 MB: digital_twin.obj, real_setup.usdz).
# Inputs to conference_table_test/build_table_parts.py and
# extract_table_textures.py only -- the scene loads the derived parts/ and
# textures/, which ARE committed. Copy these by hand if you need to rebuild.
so101_assets/conference/
